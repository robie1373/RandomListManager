{
  "name": "random-list-manager",
  "version": "1.16.1",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "sync-version": "node scripts/sync-version.js",
    "version": "npm run sync-version && git add sw.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@vitest/coverage-v8": "^4.0.17",
    "happy-dom": "^12.0.0",
    "vitest": "^4.0.17",
    "xlsx": "^0.18.5"
  }
}
